export enum Role {
  Administrator = 'administrator',
  User = 'user'
}

export enum RequestStatus {
  Pending = 'pending',
  Approved = 'approved',
  Rejected = 'rejected'
}

export enum PluginSaleStatus {
  Pending = 'pending',
  Paid = 'paid',
  Failed = 'failed',
}
